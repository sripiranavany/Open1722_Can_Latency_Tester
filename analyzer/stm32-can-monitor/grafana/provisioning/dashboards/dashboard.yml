apiVersion: 1

providers:
  - name:    STM32 CAN Monitor
    orgId:   1
    folder:  STM32
    type:    file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards